# функция из 1й лекции - для примера импора в файл второй лекции
def f(x):
    if x == 1:         # можно в одной переменной разные типы использовать (int, float,..)
        return 'Целое'
    elif x == 2.3:
        return 23
    else:
        return
#print(f(1))